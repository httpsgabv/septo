{
  "name": "septo",
  "private": true,
  "scripts": {
    "build": "turbo run build",
    "dev": "turbo run dev",
    "check-types": "turbo run check-types",
    "test": "turbo run test",
    "lint": "biome check .",
    "format": "biome check --write .",
    "codegen": "turbo run codegen",
    "test:e2e": "npm run test:e2e -w @septo/web"
  },
  "devDependencies": {
    "@biomejs/biome": "2.5.14",
    "turbo": "^2.10.13",
    "typescript": "7.0.2"
  },
  "engines": {
    "node": ">=24.7"
  },
  "devEngines": {
    "packageManager": {
      "name": "npm",
      "version": "^11.13.0"
    }
  },
  "workspaces": [
    "apps/*",
    "packages/*"
  ]
}
